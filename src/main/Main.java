package main;

public class Main {

	public static void main(String[] args) {
		
//		Konstruktor der Klasse Window wird ohne Argumente aufgerufen und neues Klassen-Obj wird erzeugt
		Window window = new Window();
//		Methode "run", aus der Klasse Window/vom Obj window, wird ausgeführt
		window.run();

	}

}
